# src/crypto_trading_bot/utils/trade_metrics.py
# src/crypto_trading_bot/utils/trade_metrics.py

"""
Trade Metrics Module

Provides functions to calculate return on investment (ROI) and other performance metrics.
"""


def calculate_roi(_):
    """
    Calculates the return on investment (ROI) for a trade.

    Args:
        trade_data (dict): Dictionary containing trade details.

    Returns:
        float: ROI value for the trade. Placeholder returns 0.0 for now.
    """
    # Placeholder implementation — this should be replaced with real calculations.
    # For now, we'll return a dummy ROI value (e.g., 0.0) so the bot runs.
    return 0.0
